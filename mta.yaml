_schema-version: "3.2"
ID: ns.zOrdenes.zOrdenes
version: 0.0.1

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
        - npm run build:workspaces

modules:
  - name: ns.zOrdenes.zOrdenes
    type: staticfile
    path: dist
    parameters:
      disk-quota: 256M
      memory: 256M